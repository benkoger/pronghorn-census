services:
  spicedb:
    container_name: prong_spicedb
    image: authzed/spicedb:latest
    environment:
      SPICEDB_GRPC_LOGLEVEL: ${SPICEDB_GRPC_LOGLEVEL}
      SPICEDB_DATASTORE_ENGINE: ${SPICEDB_DATASTORE_ENGINE}
      SPICEDB_DATASTORE_CONN_URI: ${SPICEDB_DATASTORE_CONN_URI}
      SPICEDB_GRPC_PRESHARED_KEY: ${SPICEDB_GRPC_PRESHARED_KEY}
      SPICEDB_GRPC_TLS_CERT_PATH: ${SPICEDB_GRPC_TLS_CERT_PATH}
      SPICEDB_GRPC_TLS_KEY_PATH: ${SPICEDB_GRPC_TLS_KEY_PATH}
    command: serve 
    ports:
      - "50051:50051"
      - "50052:50052"
      - "9090:9090" 
    volumes:
      - spicedb_certs:/etc/spicedb/certs:z,U

  pronghorn-api:
    container_name: prong_api
    build:
      context: .
      dockerfile: airial.containerfile
    environment:
      SECRET_KEY: ${SECRET_KEY}
      VALKEY_HOST: ${VALKEY_HOST}
      VALKEY_PASS: ${VALKEY_PASS}
      AWS_ENDPOINT_URL_S3: ${AWS_ENDPOINT_URL_S3}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS} 
      DB_PORT: ${DB_PORT}
      DB_NAME: ${DB_NAME}
      BUCKET_NAME: ${BUCKET_NAME}
      NUM_API_WORKERS: ${NUM_API_WORKERS}
      SESSION_REDIS: ${SESSION_REDIS}
      ORIGIN_URL: ${APPLICATION_URL}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      SPICEDB_GRPC_PRESHARED_KEY: ${SPICEDB_GRPC_PRESHARED_KEY}
      SPICE_URL: ${SPICE_URL}
      DB_SSL: ${DB_SSL}
      DB_SSL_MODE: ${DB_SSL_MODE}
      DB_SSL_ROOT_CERT_PATH: ${DB_SSL_ROOT_CERT_PATH}
      SPICEDB_TLS: ${SPICEDB_TLS}
    networks: 
      - pronghorn_app_network
    command: uv run gunicorn -c gunicorn.py "app:create_app()"
    volumes:
      - airial_certs:/etc/airial/certs:z,U 
      - spicedb_certs:/etc/spicedb/certs:z,U

  valkey:
    container_name: prong_valkey
    image: docker.io/valkey/valkey:8.1.1
    healthcheck:
      test: valkey-cli ping || exit 1
    restart: always
    command: valkey-server --requirepass ${VALKEY_PASS}
    networks: 
      - pronghorn_app_network 

  web-app:
    container_name: prong_webapp
    build:
      context: .
      dockerfile: production.prongwebapp.containerfile
      args:
        - VITE_API_URL_ARG: ${VITE_API_URL}
    ports:
      - "80:80"
      - "443:443"
    networks: 
      - pronghorn_app_network
    volumes:
      - nginx_conf:/etc/nginx/
      - nginx_certs:/etc/ssl/

volumes:
  nginx_conf:
    external: true
  nginx_certs:
    external: true
  spicedb_certs:
    external: true
  airial_certs:
    external: true

networks: 
  pronghorn_app_network:
    driver: bridge
